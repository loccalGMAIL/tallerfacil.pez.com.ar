# TallerFácil — Guía para Claude

## Memoria del proyecto
Toda la memoria de trabajo está en `.claude/memory/`. Leé siempre el índice al iniciar:
- [MEMORY.md](.claude/memory/MEMORY.md) — índice de archivos de memoria

## Comandos clave
```bash
composer run dev      # servidor + queue + vite (desarrollo)
php artisan migrate   # migraciones
php artisan test      # tests
```

## Reglas generales
- Responder siempre en español
- Mantener la memoria actualizada cuando cambien decisiones de arquitectura, roadmap o convenciones
